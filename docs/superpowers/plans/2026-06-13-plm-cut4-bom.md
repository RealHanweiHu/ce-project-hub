# PLM 第四刀 · BOM + where-used + 发布冻结 Implementation Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans。

**Goal:** BOM 落地：项目工作态 BOM（可改）→ 量产发布时冻结进 Revision（不可变）→ 跨产品 where-used 反查（机芯/平台被哪些整机引用）→ 版本 BOM 自动 diff。文档受控版本留 Cut 4b。

**Architecture:** 新增 `bom_items` 表（一行属于一个 project 工作态或一个 revision 冻结态；可引用零部件产品 componentProductId/componentRevisionId）。db 加 BOM CRUD + freezeBomToRevision（并入 releaseProject）+ bomDiff + whereUsed。tRPC `bom` 路由。客户端项目详情加「BOM」tab + 产品 where-used 显示。TDD，本地 docker PG。

**前置：** 本地 docker `cehub-pg`；`.env`；在 `main`，Cut 1-3 已合并。设计依据：`…two-axis…` §11 bom_items。

---

### Task 0: 分支
```bash
git checkout main && git checkout -b plm-cut4-bom
```

### Task 1: Schema — bom_items

**Files:** Modify `drizzle/schema.ts`（末尾追加）

```ts
/** BOM 行：工作态(projectId) 或 冻结态(revisionId)；可引用零部件产品 */
export const bomItems = pgTable("bom_items", {
  id: serial("id").primaryKey(),
  revisionId: integer("revisionId"),   // 冻结归属（与 projectId 二选一）
  projectId: varchar("projectId", { length: 32 }),  // 工作态归属
  partNumber: varchar("partNumber", { length: 64 }).notNull().default(""),
  name: varchar("name", { length: 256 }).notNull(),
  spec: varchar("spec", { length: 256 }).notNull().default(""),
  quantity: integer("quantity").notNull().default(1),
  refDesignator: varchar("refDesignator", { length: 128 }).notNull().default(""),
  /** 引用零部件产品（where-used 基础） */
  componentProductId: varchar("componentProductId", { length: 32 }),
  componentRevisionId: integer("componentRevisionId"),
  supplierName: varchar("supplierName", { length: 128 }).notNull().default(""),
  unitCost: varchar("unitCost", { length: 64 }).notNull().default(""),
  sortOrder: integer("sortOrder").notNull().default(0),
  createdAt: timestamp("createdAt").defaultNow().notNull(),
}, (t) => ({
  idxRevision: index("idx_bom_revision").on(t.revisionId),
  idxProject: index("idx_bom_project").on(t.projectId),
  idxComponent: index("idx_bom_component").on(t.componentProductId),
}));
export type BomItem = typeof bomItems.$inferSelect;
export type InsertBomItem = typeof bomItems.$inferInsert;
```
`pnpm check` → commit `feat(schema): add bom_items`。

### Task 2: 迁移
`pnpm db:push` → 验证 bom_items 表存在 → commit。

### Task 3: db + 测试

**Files:** Modify `server/db.ts`；Create `server/bom.test.ts`

测试要点：
- addBomLine(projectId,...) → listWorkingBom 含该行。
- updateBomLine / deleteBomLine。
- freezeBomToRevision(projectId,revId) → listFrozenBom(revId) == 工作态行数；工作态不动。
- whereUsed(componentProductId) → 含引用它的冻结 BOM 所属产品。
- bomDiff(revA,revB) → added/removed/changed 正确。

实现（追加 `server/db.ts`）：
```ts
export async function addBomLine(projectId: string, line: Partial<InsertBomItem> & { name: string }): Promise<number> {
  const db = await getDb(); if (!db) throw new Error("no db");
  const r = await db.insert(bomItems).values({ ...line, projectId, revisionId: null }).returning({ id: bomItems.id });
  return r[0].id;
}
export async function updateBomLine(id: number, patch: Partial<InsertBomItem>): Promise<void> {
  const db = await getDb(); if (!db) throw new Error("no db");
  await db.update(bomItems).set(patch).where(eq(bomItems.id, id));
}
export async function deleteBomLine(id: number): Promise<void> {
  const db = await getDb(); if (!db) throw new Error("no db");
  await db.delete(bomItems).where(eq(bomItems.id, id));
}
export async function listWorkingBom(projectId: string): Promise<BomItem[]> {
  const db = await getDb(); if (!db) return [];
  return db.select().from(bomItems).where(eq(bomItems.projectId, projectId)).orderBy(bomItems.sortOrder, bomItems.id);
}
export async function listFrozenBom(revisionId: number): Promise<BomItem[]> {
  const db = await getDb(); if (!db) return [];
  return db.select().from(bomItems).where(eq(bomItems.revisionId, revisionId)).orderBy(bomItems.sortOrder, bomItems.id);
}
/** 把项目工作态 BOM 复制冻结到某版本（用 tx 时传入 tx，否则用 db） */
export async function freezeBomToRevision(projectId: string, revisionId: number, exec?: any): Promise<unknown[]> {
  const db = exec ?? await getDb(); if (!db) throw new Error("no db");
  const rows = await db.select().from(bomItems).where(eq(bomItems.projectId, projectId));
  for (const r of rows) {
    const { id, projectId: _p, ...rest } = r as BomItem & { id: number };
    await db.insert(bomItems).values({ ...rest, projectId: null, revisionId });
  }
  return rows;
}
/** 反查：某零部件产品被哪些整机产品（冻结 BOM）引用 */
export async function whereUsed(componentProductId: string): Promise<{ productId: string; productName: string; revisionLabel: string }[]> {
  const db = await getDb(); if (!db) return [];
  const rows = await db.select({
    productId: products.id, productName: products.name, revisionLabel: productRevisions.revisionLabel,
  }).from(bomItems)
    .innerJoin(productRevisions, eq(bomItems.revisionId, productRevisions.id))
    .innerJoin(products, eq(productRevisions.productId, products.id))
    .where(eq(bomItems.componentProductId, componentProductId));
  return rows;
}
/** 两版本 BOM diff（按 partNumber+name 匹配） */
export async function bomDiff(revA: number, revB: number) {
  const a = await listFrozenBom(revA); const b = await listFrozenBom(revB);
  const key = (x: BomItem) => `${x.partNumber}|${x.name}`;
  const am = new Map(a.map((x) => [key(x), x])); const bm = new Map(b.map((x) => [key(x), x]));
  const added = b.filter((x) => !am.has(key(x)));
  const removed = a.filter((x) => !bm.has(key(x)));
  const changed = b.filter((x) => am.has(key(x)) && (am.get(key(x))!.quantity !== x.quantity || am.get(key(x))!.unitCost !== x.unitCost));
  return { added, removed, changed };
}
```
import 行加 `bomItems, BomItem, InsertBomItem`。
修改 `releaseProject` 的事务：在 insert mpReleases 前调 `const frozen = await freezeBomToRevision(input.projectId, rev.id, tx);` 并把 mpReleases.snapshotBom 设为 `frozen`。
跑 `pnpm vitest run server/bom.test.ts` → pass；全量；commit。

### Task 4: tRPC bom 路由
**Files:** Create `server/routers/bom.ts`；Modify `server/routers.ts`
- `bom.working`(query {projectId})、`bom.frozen`(query {revisionId})、`bom.add`/`update`/`delete`(mutation)、`bom.whereUsed`(query {componentProductId})、`bom.diff`(query {revA,revB})。挂载。check。commit。

### Task 5: 客户端
**Files:** Create `client/src/components/views/BomPanel.tsx`；Modify `ProjectDetailView.tsx`（加 BOM tab）；`ProductLibraryView.tsx`（零部件产品显示 where-used）
- BomPanel：项目工作态 BOM 表格，可增/删/改行（料号/名称/规格/用量/位号/供应商/单价 + 可选引用零部件产品下拉）。加 'bom' tab。
- 产品库零部件产品：版本弹窗里加「被哪些整机引用」(bom.whereUsed)。
- preview 验证：项目加几条 BOM → 发布 → 产品版本的冻结 BOM；机芯类零部件被整机引用后 whereUsed 反查。截图。
- check；commit。

### Task 6: RDS + 部署
- RDS 幂等建 bom_items + 索引 + 补迁移记录（公网端点）。部署。烟雾测试。

---
## Self-Review
- 覆盖：工作态 BOM CRUD ✓、发布冻结 ✓、where-used ✓、diff ✓。文档受控 → Cut 4b。
- 类型一致：addBomLine/.../freezeBomToRevision/whereUsed/bomDiff 定义与引用一致。releaseProject 修改点明确。
